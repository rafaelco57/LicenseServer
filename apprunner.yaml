version: 1.0
runtime: python3
build:
  commands:
    - pip install -r requirements.txt
run:
  runtime-version: 3.9
  command: uvicorn aws_service_fastapi:app --host 0.0.0.0 --port 8000
network:
  port: 8000